spring:
  messages:
    basename: application, errors

  datasource:
     url :  jdbc:h2:tcp://localhost/~/test
     username : sa
     password :
     driver-class-name : org.h2.Driver

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  thymeleaf:
    cache: false

  jpa:
    hibernate:
      use-new-id-generator-mappings : false
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
  jdbc:
    url :

logging level:
  org.hibernate.SQL : debug

admin:
  password: admin123123

kakao:
  key: 5119b65b42c4da50a4c3bb23eb9a7dd6
