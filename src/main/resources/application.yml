spring:
  messages:
    basename: application, errors

  datasource:
     url :  jdbc:h2:tcp://localhost/~/test
     username : sa
     password :
     driver-class-name : org.h2.Driver

  devtools:
  thymeleaf:
    cache: false

  jpa:
    hibernate:
      use-new-id-generator-mappings : false
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
  jdbc:
    url :

logging level:
  org.hibernate.SQL : debug

jwt:
  secret: s1BttUnQn57UZmhAynlWa85Dvk0qnzu9sa12B
